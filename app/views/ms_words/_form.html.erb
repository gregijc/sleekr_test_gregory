<%= form_for(@ms_word) do |f| %>
  <% if @ms_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ms_word.errors.count, "error") %> prohibited this ms_word from being saved:</h2>

      <ul>
      <% @ms_word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :word %><br>
    <%= f.text_field :word %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
